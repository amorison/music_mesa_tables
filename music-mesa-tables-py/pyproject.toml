[build-system]
requires = ["maturin>=0.15.1,<0.16"]
build-backend = "maturin"

[project]
name = "music-mesa-tables"
requires-python = ">=3.7"
dependencies = [
    "numpy>=1.21",
]

[tool.maturin]
profile = "release"
compatibility = "manylinux2014"
strip = true
